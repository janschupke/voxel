using UnityEngine;

namespace Voxel
{
    /// <summary>Base for category-specific actor config. Behavior type is inferred from the concrete config type.</summary>
    public abstract class ActorCategoryConfig : ScriptableObject { }
}
